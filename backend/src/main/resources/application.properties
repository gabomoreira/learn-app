spring.profiles.active=${APP_PROFILE:test}

spring.jpa.open-in-view=false

security.oauth2.client.client-id=${CLIENT_ID:learnapp}
security.oauth2.client.client-secret=${CLIENT_SECRET:learnapp123}

jwt.secret=${JWT_SECRET:batata}
jwt.duration=${JWT_DURATION:86400}